# ============================================================
# .gitignore —— 告诉 Git"哪些文件不要提交"
# ============================================================
# 为什么需要它？
#   有些文件是本机临时产物（比如系统生成的 .DS_Store、日志、依赖包），
#   提交上去只会让仓库变脏、变胖。写在这里的文件名，Git 会自动忽略。
#
# 语法说明：
#   以 / 开头 = 只忽略仓库根目录下的这个文件
#   以 / 结尾 = 忽略整个文件夹
#   * 是通配符，比如 *.log 表示"所有 .log 结尾的文件"
# ============================================================

# macOS 自动生成的文件，用来记录文件夹图标位置，跟项目无关
.DS_Store

# 编辑器/IDE 的个人配置
.vscode/
.idea/

# 日志文件
*.log
npm-debug.log*

# 依赖包目录（本项目零依赖，但以后加了 npm 就用得上）
node_modules/

# 构建产物（本项目无需构建，留着备用）
dist/
build/

# 环境变量文件，里面可能有密钥，绝对不能提交
.env
.env.local
.env.*.local

# 上一步抓小红书图片时留下的临时目录（已删可忽略）
tmp_xhs/
