# ============================================================================
# 文件：.gitignore —— 告诉 git"哪些文件不要管"
# ============================================================================
#
# 【这个文件是干嘛的？】
# Git 默认会把项目里的**所有**文件都纳入版本管理。但有些文件不该提交：
#   · 别人电脑上会自动生成的（比如 macOS 的 .DS_Store）
#   · 本地个人的编辑器配置（比如 .vscode/，你有你的习惯，同事有同事的）
#   · 体积很大、且能用命令重新生成的（比如 node_modules/、构建产物 dist/）
#
# 把它们的名字写在这个文件里，git 就会**当作没看见**，
# 既不会提交，也不会在 `git status` 里一直提示你"有未跟踪的文件"。
#
# 【语法就三条，很好记】
#   1. 一行一个规则，写文件名或路径
#   2. `#` 开头的是注释（就像这一行）
#   3. 结尾带 `/` 表示"只匹配文件夹"，比如 node_modules/
#
# 【为什么要一开始就写好它？】
# 因为如果某个文件已经被提交过一次，之后再写进 .gitignore 是**没用的** ——
# Git 会继续说"这个文件我管过了"。想补救就得用 `git rm --cached` 把它踢出去，
# 很麻烦，还容易误删同事的文件。所以一开始就配好是最省事的。
# ============================================================================


# ---------------------------------------------------------------------------
# 一、操作系统自动生成的文件
# ---------------------------------------------------------------------------

# macOS 的 Finder 会在每个你打开过的文件夹里偷偷塞一个 .DS_Store，
# 里面存的是"图标摆在哪个位置"这种纯本机的信息，对别人毫无意义。
.DS_Store
._*

# Windows 的资源管理器缩略图缓存
Thumbs.db
Desktop.ini


# ---------------------------------------------------------------------------
# 二、编辑器 / IDE 的个人配置
# ---------------------------------------------------------------------------
# 这些目录里存的是"我用什么主题、开没开自动换行"之类的个人偏好。
# 提交上去只会让每个人的设置互相打架，所以约定俗成都不提交。

.vscode/
.idea/
*.swp
*.swo
*~


# ---------------------------------------------------------------------------
# 三、依赖包和构建产物（本项目目前用不上，但先备着）
# ---------------------------------------------------------------------------
# node_modules 动辄几百 MB，而且只要 package.json 还在，
# 任何人敲一句 npm install 就能一模一样地还原出来 —— 没必要提交。
node_modules/

# 打包 / 构建的输出目录。它是"用源码算出来的结果"，
# 提交它等于把同一份东西存两遍，还容易和源码不同步。
dist/
build/


# ---------------------------------------------------------------------------
# 四、日志和临时文件
# ---------------------------------------------------------------------------

*.log
npm-debug.log*
yarn-error.log*

# 临时文件：以 _t 开头的 html（我自己调试时用的测试页，不应该进仓库）
_t*.html
